[project]
name = "q4-fastapi-redis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.139.0",
    "prometheus-client>=0.25.0",
    "redis>=8.0.1",
    "uvicorn>=0.50.2",
]
